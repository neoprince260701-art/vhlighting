# Vercel npm fix

- Node.js: 20.x
- npm: 10.8.2
- Supabase JS pinned to 2.57.0 (không dùng dấu ^)
- Install command: npm ci --no-audit --no-fund
- package-lock.json regenerated from npm registry

On Vercel, set Node.js 20.x and redeploy without build cache.
