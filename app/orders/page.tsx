"use client";
import{useEffect,useMemo,useState}from"react";import Link from"next/link";import AuthGuard from"@/components/AuthGuard";import AppShell from"@/components/AppShell";import{supabase}from"@/lib/supabase";import type{Order}from"@/lib/types";import{Plus,Search,Filter}from"lucide-react";
export default function Page(){const[d,setD]=useState<Order[]>([]);const[q,setQ]=useState('');useEffect(()=>{supabase.from('orders').select('*').order('created_at',{ascending:false}).then(({data})=>setD(data||[]))},[]);const filtered=useMemo(()=>d.filter(x=>(x.order_no+' '+x.customer_name+' '+(x.customer_phone||'')).toLowerCase().includes(q.toLowerCase())),[d,q]);return <AuthGuard><AppShell title="Danh sách đơn hàng" action={<Link className="primary inline" href="/orders/new"><Plus size={18}/>Tạo đơn hàng</Link>}><div className="panel orders-panel"><div className="orders-toolbar"><div className="search-field"><Search size={18}/><input placeholder="Tìm kiếm mã đơn, khách hàng, SĐT..." value={q} onChange={e=>setQ(e.target.value)}/></div><button className="secondary inline"><Filter size={17}/>Bộ lọc</button></div><div className="table-wrap"><table className="data-table"><thead><tr><th>Mã đơn</th><th>Khách hàng</th><th>Ngày tạo</th><th>Người lập</th><th className="align-right">Tổng tiền</th><th>Trạng thái</th><th>Thanh toán</th></tr></thead><tbody>{filtered.map(x=><tr key={x.id}><td><Link className="order-link" href={'/orders/'+x.id}>{x.order_no}</Link></td><td><b>{x.customer_name}</b></td><td>{new Date(x.created_at).toLocaleString('vi-VN')}</td><td>Vũ Lighting</td><td className="align-right"><b>{Number(x.total).toLocaleString('vi-VN')} ₫</b></td><td><span className="status success-status">Hoàn thành</span></td><td><span className="status success-status">Đã thanh toán</span></td></tr>)}{!filtered.length&&<tr><td colSpan={7} className="empty-state">Chưa có đơn hàng phù hợp.</td></tr>}</tbody></table></div></div></AppShell></AuthGuard>}
