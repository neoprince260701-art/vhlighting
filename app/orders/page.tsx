"use client";import {useEffect,useState}from"react";import Link from"next/link";import AuthGuard from"@/components/AuthGuard";import AppShell from"@/components/AppShell";import {supabase}from"@/lib/supabase";import type{Order}from"@/lib/types";import{Plus}from"lucide-react";
export default function Page(){const[d,setD]=useState<Order[]>([]);useEffect(()=>{supabase.from('orders').select('*').order('created_at',{ascending:false}).then(({data})=>setD(data||[]))},[]);return <AuthGuard><AppShell title="Đơn hàng" action={<Link className="primary inline" href="/orders/new"><Plus size={17}/>Tạo đơn</Link>}><div className="panel"><div className="table-wrap"><table><thead><tr><th>Số đơn</th><th>Khách hàng</th><th>Ngày</th><th>Tổng tiền</th><th>Trạng thái</th></tr></thead><tbody>{d.map(x=><tr key={x.id}><td><Link href={'/orders/'+x.id}><b>{x.order_no}</b></Link></td><td>{x.customer_name}</td><td>{new Date(x.created_at).toLocaleString('vi-VN')}</td><td>{Number(x.total).toLocaleString('vi-VN')} ₫</td><td><span className="badge">{x.status}</span></td></tr>)}</tbody></table></div></div></AppShell></AuthGuard>}