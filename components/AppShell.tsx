"use client";
import Link from "next/link"; import {usePathname,useRouter} from "next/navigation"; import {supabase} from "@/lib/supabase"; import {LayoutDashboard,Users,Package,ShoppingCart,Settings,LogOut,Plus} from "lucide-react";
const nav=[['/dashboard','Tổng quan',LayoutDashboard],['/customers','Khách hàng',Users],['/products','Sản phẩm',Package],['/orders','Đơn hàng',ShoppingCart],['/settings','Cài đặt',Settings]] as const;
export default function AppShell({children,title,action}:{children:React.ReactNode;title:string;action?:React.ReactNode}){const p=usePathname();const r=useRouter();return <div className="app"><aside className="sidebar"><div className="brand"><div className="logo">VH</div><div><b>VH LIGHTING</b><span>Sales Management</span></div></div><nav>{nav.map(([h,l,I])=><Link key={h} href={h} className={p.startsWith(h)?'active':''}><I size={18}/>{l}</Link>)}</nav><button className="logout" onClick={async()=>{await supabase.auth.signOut();r.replace('/login')}}><LogOut size={18}/>Đăng xuất</button></aside><main><header><div><p>HỆ THỐNG BÁN HÀNG NỘI BỘ</p><h1>{title}</h1></div>{action}</header><section className="content">{children}</section></main></div>}